import { createContext } from "react";
import { ContexType } from "../interfaces/contextype";
const INITIAL_STATE :ContexType ={
CostumerId:"",
JWT:""
}
export const  Contexto = createContext({
    
});